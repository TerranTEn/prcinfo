// Declaring a variable that can't be reassigned

const notredefinable = 10;

// Declaring a variable that can be reassigned

let reassignable = 10;
console.log(reassignable); // this prints 10
reassignable = 42;
console.log(reassignable); // this pritns 42
